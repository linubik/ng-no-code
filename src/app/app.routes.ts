import { Routes } from '@angular/router';
import { BuilderPageComponent } from './pages/builder-page/builder-page.component';

export const routes: Routes = [{ path: '', component: BuilderPageComponent }];
